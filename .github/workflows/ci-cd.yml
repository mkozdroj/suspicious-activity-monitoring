name: SAM CI/CD Pipeline

on:
  push:
    branches: [ "main", "feature/**", "fix/**" ]
  pull_request:
    branches: [ "main" ]

env:
  JAVA_VERSION: '21'
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/sam
  COVERAGE_THRESHOLD: 60

jobs:
  # ─────────────────────────────────────────────
  # STAGE 1 — BUILD
  # ─────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Make mvnw executable
        run: chmod +x mvnw

      - name: Compile (skip tests)
        run: ./mvnw compile -B --no-transfer-progress

      - name: Package JAR (skip tests)
        run: ./mvnw package -B --no-transfer-progress -DskipTests

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: sam-jar
          path: target/*.jar
          retention-days: 7

  # ─────────────────────────────────────────────
  # STAGE 2 — TEST & COVERAGE
  # ─────────────────────────────────────────────
  test:
    name: Test & Coverage
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Make mvnw executable
        run: chmod +x mvnw

      - name: Run unit tests + generate JaCoCo report
        run: |
          ./mvnw verify -B --no-transfer-progress \
            -Dspring.profiles.active=test \
            -Dspring.datasource.url="jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL" \
            -Dspring.datasource.driver-class-name=org.h2.Driver \
            -Dspring.datasource.username=sa \
            -Dspring.datasource.password= \
            -Dspring.jpa.hibernate.ddl-auto=create-drop

      - name: Publish JUnit test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: target/surefire-reports/**/*.xml
          comment_title: Unit Test Results

      - name: Check JaCoCo coverage threshold
        run: |
          REPORT="target/site/jacoco/jacoco.csv"
          if [ ! -f "$REPORT" ]; then
            echo "::error::JaCoCo report not found at $REPORT"
            exit 1
          fi
          # Sum missed + covered instruction counts across all classes
          MISSED=$(awk -F',' 'NR>1 {sum += $5} END {print sum}' "$REPORT")
          COVERED=$(awk -F',' 'NR>1 {sum += $6} END {print sum}' "$REPORT")
          TOTAL=$((MISSED + COVERED))
          if [ "$TOTAL" -eq 0 ]; then
            echo "::warning::No instructions found in JaCoCo report"
            exit 0
          fi
          PCT=$(( COVERED * 100 / TOTAL ))
          echo "Code coverage: ${PCT}% (threshold: ${{ env.COVERAGE_THRESHOLD }}%)"
          if [ "$PCT" -lt "${{ env.COVERAGE_THRESHOLD }}" ]; then
            echo "::error::Coverage ${PCT}% is below the required ${{ env.COVERAGE_THRESHOLD }}% threshold"
            exit 1
          fi

      - name: Upload JaCoCo HTML report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 14

  # ─────────────────────────────────────────────
  # STAGE 3 — CODE QUALITY (Checkstyle + PMD)
  # ─────────────────────────────────────────────
  code-quality:
    name: Code Quality Gate
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Make mvnw executable
        run: chmod +x mvnw

      - name: Run Checkstyle
        run: |
          ./mvnw checkstyle:check -B --no-transfer-progress \
            -Dcheckstyle.failsOnError=true \
            -Dcheckstyle.violationSeverity=warning \
          || echo "::warning::Checkstyle found style issues — review report"

      - name: Run PMD static analysis
        run: |
          ./mvnw pmd:check -B --no-transfer-progress \
            -Dpmd.failOnViolation=false \
          || echo "::warning::PMD found potential issues — review report"

      - name: Upload quality reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: quality-reports
          path: |
            target/checkstyle-result.xml
            target/pmd.xml
          retention-days: 14

  # ─────────────────────────────────────────────
  # STAGE 4 — DOCKER BUILD & PUSH
  # ─────────────────────────────────────────────
  docker:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [ test, code-quality ]
    # Only push images from main
    if: github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write

    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
      image_digest: ${{ steps.build-push.outputs.digest }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Download JAR artifact
        uses: actions/download-artifact@v4
        with:
          name: sam-jar
          path: target/

      - name: Set up QEMU (multi-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            # Branch-based tag: main → latest, develop → develop
            type=ref,event=branch
            # PR tag (not pushed, just labelled)
            type=ref,event=pr
            # Semantic version tag on annotated git tags
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            # Short SHA always
            type=sha,prefix=sha-,format=short

      - name: Build & push Docker image
        id: build-push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      - name: Scan image for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'    # Warn only — set to '1' to block on findings

      - name: Upload Trivy SARIF results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-results.sarif

      - name: Print image digest
        run: |
          echo "Image pushed: ${{ env.IMAGE_NAME }}"
          echo "Digest:       ${{ steps.build-push.outputs.digest }}"

  # ─────────────────────────────────────────────
  # STAGE 5 — DEPLOY TO PRODUCTION
  # ─────────────────────────────────────────────
  deploy-production:
    name: Deploy → Production
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: http://${{ secrets.PROD_HOST }}:8080/swagger-ui/index.html

    steps:
      - name: Checkout source (for docker-compose)
        uses: actions/checkout@v4

      - name: Deploy to production via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            set -euo pipefail
            echo "=== Deploying SAM to PRODUCTION ==="

            cd /opt/sam || { mkdir -p /opt/sam && cd /opt/sam; }

            # Back up current image tag for rollback
            CURRENT_TAG=$(docker inspect --format='{{index .RepoTags 0}}' sam-app 2>/dev/null || echo "none")
            echo "Previous image: $CURRENT_TAG"

            cat > .env <<EOF
            DB_HOST=db
            DB_PORT=3306
            DB_NAME=sam
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            APP_IMAGE=${{ env.IMAGE_NAME }}:latest
            SPRING_PROFILES_ACTIVE=production
            EOF

            docker compose pull app
            docker compose up -d --remove-orphans

            # Health check
            echo "Waiting for health endpoint..."
            for i in $(seq 1 24); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
                http://localhost:8080/actuator/health || echo "000")
              if [ "$STATUS" = "200" ]; then
                echo "✅ Production deployment healthy (attempt $i)"
                exit 0
              fi
              echo "  attempt $i — status $STATUS — waiting 5s..."
              sleep 5
            done

            # Rollback on failure
            echo "❌ Health check failed — rolling back to $CURRENT_TAG"
            docker compose down
            docker tag "$CURRENT_TAG" ${{ env.IMAGE_NAME }}:latest || true
            docker compose up -d
            docker compose logs --tail=50 app
            exit 1

  # ─────────────────────────────────────────────
  # POST-BUILD — Notifications
  # ─────────────────────────────────────────────
  notify:
    name: Notify on Failure
    runs-on: ubuntu-latest
    needs: [ build, test, code-quality, docker, deploy-production ]
    if: failure()

    steps:
      - name: Send Slack failure notification
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "❌ *SAM Pipeline FAILED* on `${{ github.ref_name }}`",
              "attachments": [{
                "color": "danger",
                "fields": [
                  { "title": "Repository", "value": "${{ github.repository }}", "short": true },
                  { "title": "Triggered by", "value": "${{ github.actor }}", "short": true },
                  { "title": "Commit", "value": "${{ github.sha }}", "short": false },
                  { "title": "Workflow run", "value": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}", "short": false }
                ]
              }]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
